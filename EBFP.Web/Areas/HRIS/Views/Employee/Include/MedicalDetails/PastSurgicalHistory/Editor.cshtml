
@using MVC.Series.Web.Extensions
@model EBFP.BL.HumanResources.PastSurgicalHistoryModel
@using EBFP.BL.Helper;

@using (Html.BeginCollectionItem("PastSurgicalHistory"))
{
    string formID = "HRISEB_" + Guid.NewGuid().ToString().Replace("-", "");
    <div class="divTableRow" id="@formID">
        @Html.HiddenFor(m => @Model.SH_ID, new { @class = "form-control" })
        <div class="divTableCell">
            @Html.TextBoxFor(model => model.SH_Date, "{0:MM/dd/yyyy}", new { @class = "form-control mydatepicker", @placeholder = "mm/dd/yyyy" })
        </div>

        <div class="divTableCell">@Html.TextBoxFor(m => @Model.SH_Description, new {@class = "form-control"})</div>
        <div class="divTableCell">
            <button type="button" class="btn btn-danger removeItem btn-circle MedicalAccess" id="@formID" name="@formID" data-bind="@formID"><i class="fa fa-times"></i> </button>
        </div>
    </div>
    <script type="text/javascript">

        $(document).ready(function () {
            $("#@formID .removeItem").click(function () {
                $("#" + $(this).data().bind).remove();
            });
        });
    </script>
}
